plugin for CudaText.
gives command to show Color Picker dialog.
dialog is standard from current OS.

if caret is placed on HTML color token, e.g. #f0a020 or #ab0, dialog must show this color first.
on closing Color Picker with OK, plugin inserts chosen color into text, replacing color token.

author: Alexey (CudaText)
license: MIT
